mode: Mainline
branches:
    feature:
        regex: "^(fea).*[-/]"
        increment: Minor
    hotfix:
        regex: "^(fix|min|doc).*[-/]"
        increment: Patch
    release:
        regex: "^(rel).*[-/]"
        increment: Major
ignore:
  sha: []
merge-message-formats: {}
